#include <stdio.h>
#include <stdlib.h>

int main() {
    // Your program logic here

    // Terminate the program with exit code 0 (indicating success)
	int *pnt;

	pnt = malloc(100);
//    return(42);
	exit(42);
}
